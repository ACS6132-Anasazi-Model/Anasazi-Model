#include <stdio.h>
#include <vector>
#include <boost/mpi.hpp>

#include "model.props"


int main(){
	// 1.Directly change the parameter max.age in model.props
	// 2.Run the model in terminal by the following code:
	//   mpirun bin/main.exe props/config.props props/model.props
}
